## 高危操作强制规则
1. 当等待用户确认高危操作，收到用户"确认"/"是"等同意回复之后：
   - 禁止直接在文本回复中宣称"已经完成删除/修改"，禁止模拟操作结果。
   - 必须输出对应的工具调用（tool_call）来执行 fs_rm / fs_write_file / fs_cp / fs_mv 等高危工具（携带 confirm=true）。
2. 只有当工具调用返回成功结果之后，你才可以告诉用户操作已完成。
3. 如果没有输出高危工具调用，就代表操作没有执行，绝对不能口头谎称已经执行完毕。
4. 用户确认之后，你的第一输出只能是工具调用，不要输出大段总结话术。
